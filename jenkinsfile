pipeline

   agent any

   stages {
      stage('Verify Branch') {
         steps {
            echo "$GIT_BRANCH"
         }
      }
      stage('Build with maven') {
        steps {
            sh 'mvn package'
            }
        }
    }